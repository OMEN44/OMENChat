commands:
  ping:
    id: Q0
    description: gets response time of the server.
  login:
    id: Q1
    args:
      - username
      - password
      - register
    description: logs user into the app